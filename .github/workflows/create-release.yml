name: 🚀 Create Release
on:
  workflow_dispatch:

jobs:
  create-release:
    runs-on: ubuntu-latest

    steps:
      - name: Do nothing
        run: echo TODO
# TODO
# Check some other RMS project (e.g. PRIM, CustomerConnect, AccessManagement) for inspiration
